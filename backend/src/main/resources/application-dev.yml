spring:
  datasource:
    url: jdbc:mysql://localhost:3306/intelligent_teaching_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
  
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
  
  elasticsearch:
    uris: http://localhost:9200
    username: elastic
    password: changeme

# 阿里云 OSS 配置
aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    bucket: intelligent-teaching-system
    access-key-id: your-access-key-id
    access-key-secret: your-access-key-secret

# 阿里云 DashScope 配置
dashscope:
  api-key: your-api-key

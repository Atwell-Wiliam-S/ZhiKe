spring:
  profiles:
    active: dev

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/zhike_teaching?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: zhike
    password: zhike123
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password: ''
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  elasticsearch:
    uris: http://localhost:9200

jwt:
  secret: zhike_secret_key_2026
  expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /api

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.zhike.*.entity
  configuration:
    map-underscore-to-camel-case: true

---

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://mysql:3306/zhike_teaching?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: zhike
    password: zhike123
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: redis
    port: 6379
    password: ''
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  elasticsearch:
    uris: http://elasticsearch:9200

jwt:
  secret: zhike_secret_key_2026_prod
  expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /api

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.zhike.*.entity
  configuration:
    map-underscore-to-camel-case: true